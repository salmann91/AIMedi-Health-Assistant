[phases.setup]
nixPkgs = ["tesseract", "poppler_utils", "libGL", "glib"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "streamlit run app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true"
